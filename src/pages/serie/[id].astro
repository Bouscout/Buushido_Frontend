---
import MainLayout from "../../layouts/main_layout.astro"
import Serie_accueil from "../../components/page_serie/page_serie.jsx"
// import "../../components/page_serie/serie.css"
import Footer from "../../layouts/footer.astro"
import { Nuage_9, Nuage_6 } from "../../components/accueil/decorations/decor"
import NavigationBar from "../../layouts/navigation_bar.astro"

export async function getStaticPaths(){
    const NUM_CONTAINER = 400
    // const series = await fetch('https://buushido.ml/api/build/')
    // const series_data = await series.json()
    const nombre = []
    for (let i=1; i < NUM_CONTAINER  ; i++){
        nombre.push(i.toString())
    }
//    return series_data.map(serie => ({params : {id: serie.id}, props: {serie}}))
   return nombre.map(num => ({params : {id: num}}))
}   
    // return({
    //     params : {id : serie.id},
    //     props : {serie},
    // })}
    // )
const {id} = Astro.params
// const show = await fetch('https://buushido.ml/api/serie/'+id+'/')
// const show_data = await show.json()
const pos_9 = {
    filter : 'brightness(13%)',
    top : '56%',
}
const pos_6 = {
 filter : 'brightness(50%)',
}
---

<link rel="preload stylesheet" href="/styles/serie/main.css" as="style" type="text/css">
<link rel="preload stylesheet" href="/styles/serie/small.css" as="style" type="text/css" media="screen and (max-width: 800px)">
<link rel="preload stylesheet" href="/styles/serie/load.css" as="style"  type="text/css"> 
<!-- <link rel="preload stylesheet" href="/styles/general/clouds.css" as="style" type="text/css">	 -->
<!-- <link rel="preload stylesheet" href="/styles/general/footer/upper.css" as="style" type="text/css">  -->

<MainLayout>
    <NavigationBar />
    <!-- <Nuage_6  style={pos_6}/>
    <Nuage_9  style={pos_9}/> -->
    <Serie_accueil id={id} client:only />
    <!-- <Serie_accueil data={show_data} id={id} client:only /> -->
    <Footer />
</MainLayout>